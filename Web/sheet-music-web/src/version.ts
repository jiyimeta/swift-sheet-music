/**
 * This package's npm identity, asserted by `test/version.test.ts` so the
 * toolchain is proven wired before anything harder lands on it.
 */
export const PACKAGE_NAME = "@jiyimeta/sheet-music-web";
