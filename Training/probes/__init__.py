"""Measurement probes for the raster OMR stage (P3a / P3b).

These answer, from the dataset, the questions whose wrong answers would
otherwise be discovered only after an implementation existed to be wrong.
They read `OMR_DATA_ROOT`; no dataset or corpus path is ever hard-coded.
"""
