package io.github.jiyimeta.sheetmusic.audio.model

/** Mirrors the DecodedFrame wire type. */
data class DecodedFrame(
    val x: Double,
    val y: Double,
    val width: Double,
    val height: Double,
)
